{
  "name": "subgraph",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "create-local": "graph create --node http://localhost:8020 kalam360/subgraph-test",
    "remove-local": "graph remove --node http://localhost:8020 kalam360/subgraph-test",
    "deploy-local": "graph deploy --node http://localhost:8020 --ipfs http://localhost:5001 kalam360/subgraph-test"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "^0.26.0",
    "@graphprotocol/graph-cli": "^0.28.0",
    "@graphprotocol/client-cli": "^0.0.1"
  }
}